[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "campaign-resonance-engine"
version = "0.1.0"
description = "Evidence-backed audience discovery from campaign language"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
campaign-resonance = "campaign_resonance.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

